apiVersion: v1
kind: Service
metadata:
  name:  azure-resource
  namespace: multicloud
  labels:
    app:  azure-resource
spec:
  ports:
  - port: 80
    name:  azure-resource
    nodePort: 32001
  type: NodePort
  selector:
    app:  azure-resource
